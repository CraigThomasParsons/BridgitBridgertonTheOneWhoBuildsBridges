// Package contracts defines interfaces and event types for Bridgit's event system.
//
// This package will contain emitter interfaces for publishing events when repos
// are discovered, synced, or orphaned. Currently unused but reserved for future
// event-driven architecture (e.g., triggering bridge actions on repo changes).
//
// TODO: Define EventEmitter interface for sync events.
// TODO: Add event types (RepoDiscovered, RepoSynced, RepoOrphaned).
package contracts
